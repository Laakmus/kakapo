---
import Layout from '@/layouts/Layout.astro';
import { SignupPage } from '@/components/SignupPage';

// Wymusza renderowanie po stronie serwera (potrzebne dla middleware i request headers)
export const prerender = false;

// Opcjonalnie: sprawdź czy użytkownik jest już zalogowany
// i przekieruj na /offers (wymaga implementacji auth context/cookies)
// const user = Astro.locals.user;
// if (user) {
//   return Astro.redirect('/offers');
// }
---

<Layout
  title="Rejestracja - KAKAPO"
  description="Utwórz konto w KAKAPO i zacznij wymieniać się rzeczami z innymi użytkownikami"
>
  <SignupPage client:only="react">
    <div slot="fallback" class="min-h-screen flex flex-col items-center justify-center px-4 py-12 bg-background">
      <div class="w-full max-w-md bg-card rounded-lg shadow-md p-8 border border-border text-center">
        <h1 class="text-2xl font-bold text-foreground mb-2">Rejestracja</h1>
        <p class="text-muted-foreground">Ładowanie formularza…</p>
      </div>
    </div>
  </SignupPage>
</Layout>
